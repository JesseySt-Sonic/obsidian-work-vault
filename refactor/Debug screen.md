Currently, we have a issue with debugging on both accept and prod. It would be great to have a debug like screen for (most) pages.

It would be quite easy to setup using `parallel-routes`.

## Features would include:

- cookie management
- cache management
- making request in any page
- page context based debugging

## Authentication

The simple authentication flow from Optimizly can be used to see if you are authenticated to see and toggle the debug menu.